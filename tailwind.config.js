/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Scans all files in the src directory
  ],
  theme: {
    extend: {}, // Add custom theme configurations if needed
  },
  plugins: [],
};
